{
    vault_audit_backends: vault_audit_backends_v1 {
        type
        _path
        description
        instance {
          address
        }
        options {
            ... on VaultAuditOptionsFile_v1 {
                file_path
            }
        }
    }
}
