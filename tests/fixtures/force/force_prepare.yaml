---
policies:
  - name: "policy-test-1"
    rules: |
      path "secret-test1/*" {
        capabilities = ["create", "read", "update", "delete", "list"]
      }
  - name: "policy-test-2"
    rules: |
      path "secret-test2/*" {
        capabilities = ["create", "read", "update", "delete", "list"]
      }
  - name: "policy-test-3"
    rules: |
      path "secret-test3/*" {
        capabilities = ["create", "read", "update", "delete", "list"]
      }
      path "secret-test3/secret/" {
        capabilities = ["deny"]
      }
