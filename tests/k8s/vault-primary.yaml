---
apiVersion: v1
kind: Pod
metadata:
  name: primary-vault
  labels:
    app: primary-vault
    vault-instance: primary
spec:
  containers:
  - name: vault
    image: hashicorp/vault:1.17.1
    ports:
    - containerPort: 8200
      name: http
    env:
    - name: VAULT_DEV_ROOT_TOKEN_ID
      value: root
    - name: VAULT_TOKEN
      value: root
    - name: VAULT_DEV_LISTEN_ADDRESS
      value: 0.0.0.0:8200
    - name: VAULT_ADDR
      value: http://localhost:8200
    - name: SKIP_SETCAP
      value: "true"
    - name: HOME
      value: /tmp
    volumeMounts:
    - name: tmp
      mountPath: /tmp
    livenessProbe:
      httpGet:
        path: /v1/sys/health
        port: 8200
        scheme: HTTP
      initialDelaySeconds: 15
      periodSeconds: 10
    readinessProbe:
      httpGet:
        path: /v1/sys/health
        port: 8200
        scheme: HTTP
      initialDelaySeconds: 5
      periodSeconds: 5
    resources:
      requests:
        memory: "256Mi"
        cpu: "100m"
      limits:
        memory: "512Mi"
        cpu: "200m"
  volumes:
  - name: tmp
    emptyDir: {}
---
apiVersion: v1
kind: Service
metadata:
  name: primary-vault
  labels:
    app: primary-vault
spec:
  selector:
    app: primary-vault
  ports:
  - name: http
    port: 8200
    targetPort: 8200
    protocol: TCP
  type: ClusterIP
