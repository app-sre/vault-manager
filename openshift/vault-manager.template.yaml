apiVersion: v1
kind: Template
metadata:
  name: vault-manager
objects:
- apiVersion: apps/v1
  kind: Deployment
  metadata:
    labels:
      app: vault-manager
    name: vault-manager
  spec:
    replicas: ${{REPLICAS}}
    strategy:
      type: RollingUpdate
      rollingUpdate:
        maxUnavailable: 0
        maxSurge: 1
    selector:
      matchLabels:
        app: vault-manager
    template:
      metadata:
        labels:
          app: vault-manager
      spec:
        containers:
        - image: ${IMAGE}:${IMAGE_TAG}
          imagePullPolicy: Always
          name: vault-manager
          command: ["/bin/sh"]
          args: 
          - "-c"
          - "while true; do /vault-manager {{ if ${DRY_RUN} == 'True' }}--dry-run{{ end }}; sleep ${RECONCILE_SLEEP_TIME}; done;"
          env:
          - name: GRAPHQL_QUERY_FILE
            value: ${GRAPHQL_QUERY_FILE}
          - name: GRAPHQL_SERVER_URL
            valueFrom:
              secretKeyRef:
                key: gql.base_url
                name: ${GRAPHQL_SERVER_URL}
          - name: GRAPHQL_USERNAME
            valueFrom:
              secretKeyRef:
                key: gql.username
                name: ${GRAPHQL_USERNAME}
          - name: GRAPHQL_PASSWORD
            valueFrom:
              secretKeyRef:
                key: gql.password
                name: ${GRAPHQL_PASSWORD}
          - name: VAULT_ADDR
            value: ${VAULT_ADDR}
          - name: VAULT_AUTHTYPE
            valueFrom:
              secretKeyRef:
                key: vault.auth_type
                name: ${VAULT_AUTHTYPE}
          - name: VAULT_ROLE_ID
            valueFrom:
              secretKeyRef:
                key: vault.role_id
                name: ${VAULT_ROLE_ID}
          - name: VAULT_SECRET_ID
            secretKeyRef:
                key: vault.secret_id
                name: ${VAULT_SECRET_ID}
          resources:
            requests:
              memory: ${MEMORY_REQUESTS}
              cpu: ${CPU_REQUESTS}
            limits:
              memory: ${MEMORY_LIMIT}
              cpu: ${CPU_LIMIT}
parameters:
- name: IMAGE
  value: quay.io/app-sre/vault-manager
  displayName: vault-manager image
  description: vault-manager docker image. Defaults to quay.io/app-sre/vault-manager
- name: IMAGE_TAG
  value: latest
  displayName: vault-manager version
  description: vault-manager version which defaults to latest
- name: MEMORY_REQUESTS
  value: 200Mi
- name: MEMORY_LIMIT
  value: 300Mi
- name: CPU_REQUESTS
  value: 250m
- name: CPU_LIMIT
  value: 500m
- name: REPLICAS
  value: '1'
- name: RECONCILE_SLEEP_TIME
  value: '15m'
- name: DRY_RUN
  value: 'False'