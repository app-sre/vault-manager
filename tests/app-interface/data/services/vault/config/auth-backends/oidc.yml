---
$schema: /vault-config/auth-1.yml

labels:
  service: vault.local

_path: "oidc/"
type: "oidc"
description: "rh sso auth backend"
settings:
  config:
    _type: "oidc"
    oidc_discovery_url: "http://localhost:8080/auth/realms/test"
    oidc_client_id: "vault"
    oidc_client_secret: "placeholder"
    default_role: "default"